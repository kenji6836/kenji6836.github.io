# MAKING #02 リーチラボ（Web アプリ）を Claude Code で作る — 制作報告

状態: **完成（イントロ/アウトロ統合済み・2026-09-14 20:47 JST）**。`site/src/v/brand/intro-outro.css`（20:37 版）を `<style>` にコピー、`intro-outro.html` の 2 つの `<section>` を `.intro-wrap`（z=80）／`.outro-wrap`（z=70）に埋め込み（本編の `.wipe` z=40 より上）。通番は `.v-no` を「#02」に、アウトロは `--v-outro-start:23s`。22.45–23.0s は本編側の青→紺サークルワイプで紺地を先出しし、23.0s からアウトロ要素が動く。

## 成果物（`site/src/v/shorts/02/`）
- `short.html` — 1080×1920・30fps・26.0s・`window.__duration=26`・`__ready/__seek`（promo.html と同方式・JS タイマー不使用）
- `out/v-short-02-reachlab.mp4`（H.264 crf20 + AAC 128k・BGM mux 済み）／`out/v-short-02-reachlab.webm`（VP9 crf32 + Opus 96k）
- `out/v-short-02-reachlab-poster.jpg`（t=3.0s・品質 85）
- `out/sb-1..6.jpg`（540×960）＋ `storyboard.json`（src は `/assets/works/v-short-02-sb-N.jpg`）
- BGM: `../bgm/making-26s.wav`（26.0s・44.1kHz stereo）を `-shortest` で mux

## 構成表（本編 5 ビート）
| 時刻 | 字幕（白 900・70px・2 行以内・y=1180–1460） | 画面 |
|---|---|---|
| 0.0–2.0 | — | ブランド イントロ: 琥珀帯→マーク落下→「MAKING #02」＋カーソル→「AIで作って、本番に出す。」→1.75s でフェード |
| 2.0–5.2 | 「ブラウザで動く／演出シミュレーターを、」→「**Claude Code** で作った。」（Claude Code は青面白抜き） | 実戦画面 `reachlab-play-390.png` を 692×1597 に拡大してスマホ枠に入れ、下からスッと入る。字幕帯（紺 86%）で可読性確保 |
| 5.05–9.65 | 「まず、**日本語**で頼む」（日本語＝琥珀） | 端末パネル（紺より明るい面・Plex Mono）。`$ claude` → `❯` に FACTS の指示（56 文字）が 5.7s から 56ms/文字で 1 文字ずつ（各文字 `steps(1,end)` の animation-delay・カーソルは文字ごとの `::after` が追従→打ち終わりで点滅） |
| 9.7–14.7 | 「抽選エンジンと／演出を**分けて設計**」 | 3 層ブロック図: 抽選エンジン（琥珀アイコン・9.7s）→演出（10.4s）→記録（11.1s）が組み上がり、矢印が描画（11.0/11.7s）・ラベル「結果だけ渡す」「回転数・当たり」・琥珀ドットが流れる（12.4s〜） |
| 14.55–18.65 | 「自動テスト **235** 項目を／毎回通す」 | テストパネル: 抽選エンジン 0→103・ブラウザ自動操作 0→132・公開前チェック preflight 0→12 がバー＋数字でカウントアップ（`@property --n` + `counter()`）→各行に緑チェック→合計チップ「自動テスト 235 項目」が 17.05s で緑に |
| 18.5–23.0 | 「**公開**して、スマホで遊べる」（公開＝琥珀） | 公開ページ `reachlab-top-390.png` を 512 幅でスマホ枠に入れ 18.9–22.7s に 1000px 等速スクロール。チップ「外部依存ゼロ」(19.6s)「PWA・オフライン起動」(20.3s)「2026-08-07 公開」(21.0s)。22.45–23.0s に青→紺のサークルワイプ |
| 23.0–26.0 | — | ブランド アウトロ: マーク→ Kenji Ido → @idk0723ai →「作った実物は HP で」→ kenji6836.github.io（琥珀カーソル点滅） |
| 5.0–22.3（装飾） | — | 下帯（y=1585〜・安全域外）: 5 ビートの進捗セグメント＋ `reachlab.pages.dev`（Plex Mono 40px） |

## 使った実数と出典（FACTS.md ✅ 一次 = リポジトリ ~/Workspace/pachi-sim・メモリ project_pachi_sim）
- 自動テスト 235 項目（エンジン 103 + ブラウザ自動操作 132）／公開前チェック preflight 12 項目／外部依存ゼロ／PWA（ホーム画面に追加・オフライン起動）／2026-08-07 公開／URL reachlab.pages.dev（FACTS 題材）
- 図の文言「当落を先に決める」「結果の見せ方」「乱数」はキャプチャ内の透明性ポリシー①④（`reachlab-top-390.png`）に基づく。「2 機種・7 ページ」は使っていない（画面内に 2 機種が映る）
- 指示文 = FACTS「指示の例（要旨）」そのまま

## 表現の注意への対応
- 技術（設計・テスト・PWA・配信）が主役。当たり・出玉の強調なし（実戦画面は静止・数字ゼロの状態）。ギャンブル的な煽り文言なし
- 生成イラストなし: 実画面キャプチャ 2 枚 + CSS/SVG（輪郭なし・2 段階陰影・線画アイコン stroke 1.75）のみ。写真は不使用
- 禁止語（副業/会社員/想定案件/モック/準備中）なし。数字は FACTS の実数のみ
- 安全域: 字幕 y=1180–1460・重要要素は上 220/下 420/右 120 を回避（右端の最大 = 端末パネル 960px・チップ 950px）。スマホ枠の上下端と下帯は装飾扱い

## 試し撮り・自己確認（H5）
- 1 回目 `--frames 0,60,100,150,210,300,330,390,450,510,540,600,630,675,690,750`: 矢印の先端（stroke-dasharray 60 < パス長 68）が描画前から 8px 見えていた→ dasharray 100 に修正＋ SVG を `.in fade` で包む。ラベル右端 994px→短文化して 912px 以内
- 2 回目 `--frames 90,240,285,360,420,480,570,600,665`: 文字の重なり・はみ出し・豆腐・画像未ロードなし。カウントアップ（82/51/104）・カーソル追従・スクロール・チップ位置を目視確認
- 全 780 フレーム→ffmpeg エンコード後、フレーム PNG は削除済み（下記「エンコード結果」参照）

## 未確認
- イントロ/アウトロ統合の確認: `--frames 0,15,30,45,59,61,689,692,705,735,765,779` の試し撮り＋最終 mp4 の 1.2s/24.5s を Read で確認（初回は intro-outro.html 先頭コメント内の `<section …>` 文字列を正規表現が拾い DOM が壊れた→コメント除去後に再抽出して解消）。全 780 フレームの通し目視はしていない（代表フレームのみ）
- brand.css 未着のため色トークンは tokens-provisional.css と同値をインライン定義（差し替え時に `:root` を置換）
- 実機（Shorts/Reels/TikTok）の UI 重なりは 🟡 一般的な安全域で回避。実投稿での見え方は未検証
- BGM は本体合成の wav をそのまま mux（音量・タイミングの調整はしていない）

## エンコード結果（2026-09-14 20:47 JST・統合版）
- `out/v-short-02-reachlab.mp4` 3.5MB・h264 1080×1920・780 フレーム・26.000s・AAC（mean −17.8 dB / max −1.1 dB）
- `out/v-short-02-reachlab.webm` 2.7MB・vp9 + opus・26.008s
- ポスター = frame 90（t=3.0s）／絵コンテ = frame 90・240・405・528・636・735（t=3.0／8.0／13.5／17.6／21.2／24.5s・sb-6 はアウトロ）
- 統合前の初回エンコード（19:34）は紺スロット版。統合後 20:43 に全 780 フレームを `--from 0 --to 389`／`--from 390 --to 779` で再レンダーして再エンコード（フレーム削除済み）
- 全フレーム render は 1 回目に frame 82 で CDP 応答が止まった（Chrome 0% CPU・原因未特定・他レーンの render と同時刻）→ 自分のプロセスだけ kill し `--from 82 --to 300`／`--from 301 --to 779` で分割再実行して完走。frames/ と中間 silent.mp4 は削除済み
