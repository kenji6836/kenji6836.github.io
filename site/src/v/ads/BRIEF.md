# 広告セット（題材 = ショーケース A『見積トリアージ』）— ブリーフ

## 題材（A レーン確定・2026-09-14 20:3x）
- 製品名: **見積トリアージ**（英字 MITSUMORI TRIAGE）。建設向け「見積依頼メールの AI 仕分け＋概算返信の下書き」Web アプリ（自作・PWA・公開先 `/app/mitsumori/`・デモ会社名「サンプル工務店」）。タグライン案「見積依頼を 60 秒で仕分け、概算返信の下書きまで」
- トークン: `a-input/tokens-draft.css`（主色 #2457e6・白基調 #f4f6fa・文字 #14171f・Noto Sans JP・角丸 14/10・至急=赤 #c8232b・未対応=琥珀 #b7791f・返信済=緑 #1a7f4b）。製品アイコン: `a-input/icon.svg`（青角丸に白線画の受信箱＋琥珀の丸）。代表画面: `a-input/mock-a-1280.png`（3 ペイン・白基調。**この画面を切り出してブラウザ/端末枠に入れて使ってよい**＝製品の実物感）
- 写真（実写風・自主制作）: `../../photos/` の `kensetsu-office-desk.jpg`（事務所で紙の依頼を確認＝Before）・`kensetsu-phone-inbox.jpg`（スマホで依頼を見る手元）・`kensetsu-site-tablet.jpg`・`kensetsu-worker-portrait.jpg`（縦）・`kensetsu-exterior.jpg`。**写真は最初から混ぜる**（イラストのみは不可・ユーザー訂正 9/13）。写真の切り出しは cv2 か CSS `object-fit` で

## 訴求 2 案（A/B・レイアウト骨格は共通）
- **A 時間訴求**: 見出し「見積依頼の仕分け、60 秒。」／本文「届いた依頼を AI が 緊急度・工種・概算帯 で仕分け。概算返信の下書きまで自動。」／CTA「デモを触る」
- **B 安心訴求**: 見出し「急ぎの依頼、埋もれない。」／本文「朝の受信箱を自動で仕分け。至急だけが一番上に来る。」／CTA「実物を見る」
- 共通: 製品名＋アイコン・小さく「建設業向け」・右下または下端に「自主制作のサンプル広告（未出稿）」の注記（小さく・読める）。数字は「60 秒」以外に出さない（実測ではなく設計目標なので、本文では「目安」と添える）

## サイズ（7 点・書き出しは `out/`・JPEG q88・ファイル名固定）
`ad-a-square-1080.jpg`(1080×1080 A)・`ad-b-square-1080.jpg`(1080×1080 B)・`ad-a-story-1080x1920.jpg`・`ad-a-landscape-1200x628.jpg`・`ad-a-rect-300x250.jpg`・`ad-a-leaderboard-728x90.jpg`・`ad-a-x-1600x900.jpg`。小サイズ（300×250・728×90）は見出し＋CTA＋アイコンだけに絞る。CTA は全サイズで同じ位置ルール（右下 or 下端中央）。文字の最小サイズ: 1080 幅で 36px・300×250 で 14px・728×90 で 16px
実装: 各サイズを 1 つの `banner.html` に `<section data-size>` として並べ、`node site/tools/cdp-render.mjs banner.html <w> <h> out/<file> --selector "[data-size='…']" --wait 2500` で切り出す（既存 `site/src/ad/banner.html` の作り方を参考に）

## 比較ページ `compare.html`（自己完結・Google Fonts のみ・相対パス・ライト/ダーク・1,500 字以内）
上: A/B を並べて訴求の違いを 3 行で／中: 7 サイズを実寸比で一覧（小サイズは等倍）／下: 「A/B テストの見方（サンプル数値と明記）」と「ヒアリング→訴求軸→サイズ展開→入稿」の 4 ステップ。将来 `/ads/mitsumori/` として公開する前提

## 報告
`REPORT.md`（成果物・自己検査: 文字の重なり/はみ出し/最小文字/注記の有無・未確認）。書き出し画像は必ず自分で Read して確認。返答は要約 10 行以内
