# site/src/photos — 実写風写真（レーン V が生成・W/A/広告で共用）
- 一覧と用途: `manifest.json`／生成プロンプト: `prompts.json`／生成ログ: `work/run.log`
- 全て自主制作（ChatGPT 画像生成 → degrain 後処理・本人写真なし・画像内テキストなし）。顧客実績として提示しない
- 使い方: `<img src="/site/src/photos/…">` ではなく、各レーンが必要サイズに切り出して `assets/` へ書き出す（原本は 1536px・JPEG q92）
